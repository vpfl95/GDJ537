<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.app.home.user.UserMapper">
	<insert id="setJoin" parameterType="UserVO">
		INSERT INTO USER VALUES(null,#{id},1,1,#{pw},#{name},#{email},#{phone},now(),"asd","asd");
	</insert>

	<select id="getLogin" parameterType="String" resultMap="getLoginResult">
		SELECT U.*, R.*
		FROM USER U
			INNER JOIN
			ROLE R
			ON U.ROLENUM = R.ROLENUM
		WHERE U.id=#{username}
	</select>
	<resultMap type="UserVO" id="getLoginResult">
		<id column="num" property="num"/>
		<result column="id" property="id"/>
		<result column="pw" property="pw"/>
		<result column="name" property="name"/>
		<result column="email" property="email"/>

		<collection property="roleVOs" javaType="List" ofType="RoleVO">
			<id column="roleNum" property="roleNum"/>
			<result column="roleName" property="roleName"/>
		</collection>
	</resultMap>

</mapper>    